{
  "name": "sysco-ordering-bot",
  "version": "0.1.0",
  "description": "sysco ordering using api.ai",
  "main": "webhook.js",
  "scripts": {
    "start": "nf start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "git add . && git commit -m 'deploying' && git push && git push heroku master",
    "local": "./ngrok http 5000 && nodemon ./webhook.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nicholas-b-carter/fb-apiai-bot-demo"
  },
  "author": "Nick Carter",
  "license": "MIT",
  "dependencies": {
    "api-ai-botkit": "^1.0.3",
    "botkit": "^0.5.5"
  }
}
